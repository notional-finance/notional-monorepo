import { LEND_BORROW } from '@notional-finance/shared-config';
import { MaturityData } from '@notional-finance/notionable';

export function useManageMaturities(_borrowOrLend: LEND_BORROW) {
  return [] as MaturityData[];
}
