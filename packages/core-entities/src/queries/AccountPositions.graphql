query AccountPositions($account: String!, $skip: Int) {
  balances(
    where: { account: $account, current_: { currentBalance_not: 0 } }
    first: 1000
    skip: $skip
  ) {
    id
    token {
      id
    }
    current {
      currentBalance
    }
  }
}
