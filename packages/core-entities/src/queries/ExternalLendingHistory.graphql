query ExternalLendingHistory {
  externalLendings {
    id
    underlying { id }
    underlyingSnapshot (orderBy: timestamp, orderDirection:desc, first: 10) {
      timestamp
      balanceOf
      storedBalanceOf
    }
  }

  _meta {
    block {
        number
    }
  }
}
