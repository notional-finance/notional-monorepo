query AllVaultAccounts($skip: Int) {
  balances(
    where: {
      token_: { tokenType: VaultShare }
      current_: { currentBalance_not: 0 }
    }
    first: 1000
    skip: $skip
  ) {
    account {
      id
    }
    token {
      id
    }
    current {
      currentBalance
    }
  }
}
